{
  "name": "phaser_accessibility_spike",
  "version": "1.0.0",
  "description": "test spike for accessibility trials",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc",
    "rjs": "r_js -o scripts/build.js",
    "build": "tsc;cp ./node_modules/phaser-ce/build/phaser.min.js intermediate;r_js -o scripts/build.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^2.5.3"
  },
  "dependencies": {
    "phaser-ce": "^2.9.1",
    "requirejs": "^2.3.5"
  }
}
